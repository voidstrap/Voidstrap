<Page
    x:Class="Voidstrap.UI.Elements.Settings.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Voidstrap.UI.Elements.Controls"
    xmlns:settings="clr-namespace:Voidstrap.UI.ViewModels.Settings"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:resources="clr-namespace:Voidstrap.Resources"
    xmlns:models="clr-namespace:Voidstrap.UI.ViewModels"
    Title="History">

    <Grid Margin="0,0,28,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock
    FontSize="14"
    Margin="0,0,0,8"
    TextWrapping="Wrap"
    Foreground="{StaticResource TextFillColorSecondaryBrush}"
    Text="Continue where you left off" />

            <ui:Button Grid.Column="1"
                       Content="Refresh"
                       Appearance="Secondary"
                       Command="{Binding RefreshCommand}"/>

            <ui:Button Grid.Column="3"
                       Content="Clear All"
                       Appearance="Secondary"
                       Command="{Binding ClearCommand}"/>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding Error}"
                   Foreground="Red"
                   FontSize="12"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Error}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Grid Grid.Row="2">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsEmpty,
                            Converter={StaticResource BooleanToVisibilityConverter}}">
                <ui:SymbolIcon Symbol="History24" FontSize="52" Opacity="0.25"
                               HorizontalAlignment="Center"/>
                <TextBlock Text="No history yet"
                           FontSize="16" Opacity="0.5"
                           Margin="0,12,0,4" HorizontalAlignment="Center"/>
                <TextBlock Text="Games you play will appear here."
                           Opacity="0.4" FontSize="12"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding IsEmpty,
                              Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding GameHistory}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card Margin="0,0,0,6" Padding="12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="56"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0"
                                            Width="48" Height="48"
                                            CornerRadius="6"
                                            ClipToBounds="True"
                                            Background="#1AFFFFFF">
                                        <Grid>
                                            <ui:SymbolIcon Symbol="Games28"
                                                           FontSize="22"
                                                           Opacity="0.4"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>

                                            <Image Stretch="UniformToFill"
                                                   Source="{Binding UniverseDetails.Thumbnail.ImageUrl}">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding UniverseDetails}"
                                                                         Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding UniverseDetails.Thumbnail}"
                                                                         Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </Grid>
                                    </Border>

                                    <StackPanel Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Margin="12,0,12,0">

                                        <TextBlock FontSize="14" FontWeight="Medium">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text"
                                                            Value="{Binding UniverseDetails.Data.Name}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding UniverseDetails}"
                                                                     Value="{x:Null}">
                                                            <Setter Property="Text"
                                                                    Value="{Binding PlaceId,
                                                                        StringFormat='Place {0}'}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <TextBlock FontSize="12" Opacity="0.55" Margin="0,3,0,0">
                                            <Run Text="Joined: "/>
                                            <Run Text="{Binding TimeJoined,
                                                         StringFormat='{}{0:MMM d, yyyy  h:mm tt}',
                                                         Mode=OneWay}"/>
                                        </TextBlock>

                                        <TextBlock Text="{Binding GameHistoryDescription}"
                                                   FontSize="11"
                                                   Opacity="0.4"
                                                   Margin="0,3,0,0"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding GameHistoryDescription}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding GameHistoryDescription}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">

                                        <ui:Button Content="Copy Link"
                                                   Appearance="Secondary"
                                                   Margin="0,0,6,0"
                                                   Command="{Binding DataContext.CopyLinkCommand,
                                                       RelativeSource={RelativeSource AncestorType=Page}}"
                                                   CommandParameter="{Binding}"/>

                                        <ui:Button Content="Play"
                                                   Appearance="Primary"
                                                   Command="{Binding DataContext.LaunchCommand,
                                                       RelativeSource={RelativeSource AncestorType=Page}}"
                                                   CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>